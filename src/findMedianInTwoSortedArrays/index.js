// 这个题目说的是，给你两个排好序的整数数组 nums1 和 nums2，假设数组是以递增排序的，数组的大小分别是 m 和 n。你要找到这两个数组的中位数。要求算法的时间复杂度是 O(log(m+n))。
//
// 这里两个数组中位数的意思是，两个数组合到一起排序后，位于中间的那个数，如果一共有偶数个，则是位于中间的两个数的平均数。
//
// 比如说，给你的两个数组是：
//
// 1, 3
// 2
//
// 它们放在一起排序后是：
//
// 1, 2, 3
//
// 所以中位数就是 2。
//
// 再比如说，给你的两个数组是：
//
// 1, 3
// 2, 4
//
// 它们放在一起排序后是：
//
// 1, 2, 3, 4
//
// 所以中位数就是 (2 + 3) / 2 = 2.5。

const arr1 = [1, 3];
const arr2 = [2, 4];

const findMedianInTwoSortedArrays = (a1, a2) => {
  let find;
};

const findKthSmallestInSortedArrays = (a1, a2, k) => {
  const len1 = a1.length;
  const len2 = a2.length;

  let base1 = 0;
  let base2 = 0;


};
